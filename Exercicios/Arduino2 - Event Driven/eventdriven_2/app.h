/* chama aplicação para iniciar configuração */
void appinit(void);
/* avisa que ’pin’ mudou para valor ’v’ */
void button_changed (int pin, int v);
/* avisa que timer ’t’ expirou */
void timer_expired(int t);